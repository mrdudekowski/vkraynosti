/**
 * Горизонтальный sheen на линиях и обложке `TourCardSkeleton`.
 * Синхронно с `keyframes.tour-card-skeleton-sheen` и `animation.tour-card-skeleton-sheen` в `tailwind.config.ts`.
 */
export const TOUR_CARD_SKELETON_SHIMMER_MS = 1600 as const;
