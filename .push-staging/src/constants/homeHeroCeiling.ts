/**
 * Потолок скролла у якоря hero на главной.
 * Длительность keyframes `home-hero-ceiling-bounce` в `tailwind.config.ts` — только для возможного reuse в UI.
 */

export const HOME_HERO_CEILING_BOUNCE_DURATION_MS = 640;

/** Допуск сравнения с линией потолка (px). */
export const HOME_HERO_CEILING_EPSILON_PX = 0.5 as const;
