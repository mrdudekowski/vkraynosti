/**
 * Типографика, заданная в `tailwind.config.ts` (`theme.extend.fontSize`), не дублируется здесь.
 *
 * Hero главной: заголовок — `text-home-hero-document-title` + `font-hero-heading`;
 * фраза тура — `text-home-hero-carousel-phrase` + `font-hero-carousel-phrase` (`HeroCarousel`).
 * Загрузка шрифтов: `src/constants/fonts.ts`, `bootstrap-fonts.ts`.
 */
