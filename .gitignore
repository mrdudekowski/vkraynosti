# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

.env
.env.*
!.env.example
!.env.cms-dev.example

# Dependencies (never commit)
node_modules/
.npm-cache/
node_modules.partial/

dist
dist-admin/
coverage
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Cursor local debug dumps (rules/settings may stay tracked for the team)
.cursor/debug*.log
.cursor/plans/
.cursor/preview-ezh/
.cursor/rules/ponytail.mdc

# GAS integrations: tests and Node mirrors (not web-vkr deploy / npm run build)
integrations/tour-schedule-gas/__tests__/
scripts/lib/previousMonthBounds.mjs

# Local media audit utility (not in npm run build / CI)
scripts/zip-used-site-images.mjs

# Local CDN catalog snapshots — deploy/CI: npm run sync:catalog (not committed on web-vkr)
public/data/tour-schedule/
public/data/tour-schedule.json
public/data/cms/

# CMS-dev export scratch
/tmp/

# TimeWeb media inventory dumps (regenerate: npm run media:inventory)
TimeWebDoc/audit-*.json

# PDF generator preview (regenerate: npm run generate:manager-guide-pdf)
docs/.tmp-manager-guide-preview.html

# Windows folder metadata (never deploy)
**/desktop.ini
**/Thumbs.db

# Исходники фото/видео туров (не в репозитории)
/content/

# web-vkr only: tour media on S3 — SSOT and public/ copies live on main (npm run sync:s3)
public/tours/
public/team/
public/banners_winter/
public/banners_spring/
public/banners_summer/
public/banners_fall/
# safety.webp и прочее — S3; иконки статусов — в App (public/safety/icons/)
public/safety/*
!public/safety/icons/
!public/safety/icons/**

# Local measurement / audit dumps (not source of truth; regenerate locally or in CI artifacts)
/perf-*.json
/lighthouse-report*.json
/lighthouse-*.json
/.lighthouse*.json
/video-audit*.json
/phase4-network-snapshot.json
/test-results/
/playwright-report/
/blob-report/
*.tsbuildinfo

# Internal docs & audits (not imported by build/test; keep locally)
/docs/
/optimize/

# Markdown outside repo root (README.md at / is allowed)
**/*.md
!/*.md
!TimeWebDoc/vkraynosti-ru-domain-cutover.md

# Template / temporary project artifacts
/pred-plan.md
/public/vite.svg
/public/spring tours/

# Local recovery / transcript dumps (not source of truth)
/recovered-final/
/recovered-team/
/extracted-from-transcript/
/extracted-writes/
/.superpowers/
