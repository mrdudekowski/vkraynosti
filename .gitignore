# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

.env
.env.*
!.env.example

# Dependencies (never commit)
node_modules/

dist
coverage
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Cursor local debug dumps (rules/settings may stay tracked for the team)
.cursor/debug*.log
.cursor/plans/
.cursor/preview-ezh/

# TimeWeb media inventory dumps (regenerate: npm run media:inventory)
TimeWebDoc/audit-*.json

# Windows folder metadata (never deploy)
**/desktop.ini
**/Thumbs.db

# Исходники фото/видео туров (не в репозитории)
/content/

# web-vkr only: tour media on S3 — SSOT and public/ copies live on main (npm run sync:s3)
public/tours/
public/team/
public/banners_winter/
public/banners_spring/
public/banners_summer/
public/banners_fall/
public/safety/

# Local measurement / audit dumps (not source of truth; regenerate locally or in CI artifacts)
/perf-*.json
/lighthouse-report*.json
/lighthouse-*.json
/.lighthouse*.json
/video-audit*.json
/phase4-network-snapshot.json
/test-results/
/playwright-report/
/blob-report/
*.tsbuildinfo

# Internal docs & audits (not imported by build/test; keep locally)
/docs/
/optimize/

# Markdown outside repo root (README.md at / is allowed)
**/*.md
!/*.md

# Template / temporary project artifacts
/pred-plan.md
/public/vite.svg
/public/spring tours/

# Local recovery / transcript dumps (not source of truth)
/recovered-final/
/recovered-team/
/extracted-from-transcript/
/extracted-writes/
