/**
 * `IntersectionObserver.rootMargin` для `CrossfadeVideo` (карточка «ещё туров» на главной).
 * Совпадает с запасом до монтирования медиа в `Home.tsx` (`expandCardMediaViewportRef`).
 */
export const CROSSFADE_VIDEO_INTERSECTION_ROOT_MARGIN = '300px 0px' as const;
