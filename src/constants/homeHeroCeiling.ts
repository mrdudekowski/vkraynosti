/**
 * Длительность keyframes `home-hero-ceiling-bounce` в `tailwind.config.ts`.
 * Остальная логика отскока hero удалена; анимация оставлена для возможного reuse.
 */
export const HOME_HERO_CEILING_BOUNCE_DURATION_MS = 640;
